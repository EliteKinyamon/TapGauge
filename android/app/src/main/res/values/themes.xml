<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme; actual colors come from Compose Material3 at runtime.
         We only need a launch/splash-safe Android theme here. -->
    <style name="Theme.TapGauge" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>
</resources>
